---
services:
  pyload-ng:
    image: lscr.io/linuxserver/pyload-ng:latest
    container_name: pyload-ng
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=America/Chicago
    volumes:
      - /home/srv/services/pyload-ng/config:/config
      - /home/stephen/Downloads:/downloads
    ports:
      - 8002:8000
    restart: unless-stopped
