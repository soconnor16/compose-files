services:
  filebrowser:
    image: filebrowser/filebrowser:latest
    restart: unless-stopped
    volumes:
      - /srv/services/filebrowser/data:/srv
      - /srv/services/filebrowser/database.db:/database.db
    networks:
      - services

networks:
  services:
    external: true
