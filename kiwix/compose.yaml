services:
  kiwix:
    container_name: kiwix
    image: ghcr.io/kiwix/kiwix-serve:latest
    restart: unless-stopped
    volumes:
      - /services/kiwix/data:/data
    command: "*.zim"
    networks:
      - services

networks:
  services:
    external: true
